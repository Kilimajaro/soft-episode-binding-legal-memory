# Web UI
gradio>=4.0.0,<6.0.0

# HTTP (Ollama API)
requests>=2.28.0,<3.0.0

# Numerics & vectors
numpy>=1.24.0,<3.0.0

# Vector search (use faiss-gpu instead if you have a matching CUDA build)
faiss-cpu>=1.7.4

# Clustering (BIRCH, KMeans, silhouette)
scikit-learn>=1.3.0,<2.0.0

# Eval / scripts
tqdm>=4.65.0,<5.0.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
